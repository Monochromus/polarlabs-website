{
  "name": "polarlabs-website",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:prerender": "tsc && vite build && node scripts/prerender.mjs",
    "prerender": "node scripts/prerender.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "puppeteer": "^23.0.0",
    "typescript": "^5.3.3",
    "vite": "^5.4.0"
  }
}
